databases:
  - name: stms-db
    plan: free
    databaseName: stms
    user: stms_user
    region: singapore
    ipAllowList: [] # Allow all connections (Render internal)

services:
  - type: web
    name: stms-backend
    runtime: node
    plan: free
    region: singapore
    buildCommand: npm ci && npm run build
    startCommand: node dist/main
    healthCheckPath: /api/v1/health
    numInstances: 1
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: "10000"
      - key: DB_HOST
        fromDatabase:
          name: stms-db
          property: host
      - key: DB_PORT
        fromDatabase:
          name: stms-db
          property: port
      - key: DB_USERNAME
        fromDatabase:
          name: stms-db
          property: user
      - key: DB_PASSWORD
        fromDatabase:
          name: stms-db
          property: password
      - key: DB_DATABASE
        fromDatabase:
          name: stms-db
          property: database
      - key: DB_SSL
        value: "true"
      - key: DATABASE_URL
        fromDatabase:
          name: stms-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: JWT_EXPIRES_IN
        value: 1d
      - key: REDIS_HOST
        value: ""
      - key: REDIS_PORT
        value: "6379"
      - key: CORS_ORIGINS
        value: "https://stms-frontend.onrender.com"
